#!/bin/bash -e

bench=$1
algo=$2

mb=/z/int/muscle_benchmark

accs=../accs/$bench

## for acc in `cat ../accs/$bench`
## do
	## ./daliz $bench $algo $acc
## done

cat ../accs/$bench \
  | parallel -j 20 ./daliz $bench $algo {}
