#!/bin/bash

acc=$1

if [ x$acc == x ] ; then
	echo Missing arg
	exit 1
fi

cd /z/int/muscle_benchmark2/bbs

outdir=../muscle5_mega_bbs
mkdir -p $outdir

out=../muscle5_mega_bbs/$acc
muscle -align ../bbs_megas/$acc.mega -output $out
